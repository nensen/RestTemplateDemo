ABOUT:
Simple script that can be used as a template for CRUD operations to the REST API endpoint. Can be run from the powershell. 
The processing logic is in main.js file, data that will be sent to API are stored in JSON format in Script/dataModel.js

HOW TO RUN:
run run_seed.ps1 from powershell terminal window